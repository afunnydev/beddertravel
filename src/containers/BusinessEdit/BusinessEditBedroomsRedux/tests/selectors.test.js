// import { fromJS } from 'immutable';
// import { selectBusinessAddBedroomsReduxDomain } from '../selectors';

describe('selectBusinessAddBedroomsReduxDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
