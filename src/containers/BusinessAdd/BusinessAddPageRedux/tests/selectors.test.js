// import { fromJS } from 'immutable';
// import { selectBusinessAddPageReduxDomain } from '../selectors';

describe('selectBusinessAddPageReduxDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
