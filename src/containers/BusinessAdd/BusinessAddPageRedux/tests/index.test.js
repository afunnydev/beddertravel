// import React from 'react';
// import { shallow } from 'enzyme';

// import { BusinessAddPageRedux } from '../index';

describe('<BusinessAddPageRedux />', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
