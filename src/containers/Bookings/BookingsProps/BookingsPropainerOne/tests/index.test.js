// import React from 'react';
// import { shallow } from 'enzyme';

// import { BookingsPropainerOne } from '../index';

describe('<BookingsPropainerOne />', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
